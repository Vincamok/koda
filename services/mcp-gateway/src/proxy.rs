/// Futur serveur HTTP/SSE exposant le protocole MCP natif pour les connecteurs stdio externes.
/// Phase actuelle : le transport passe par Redis Streams (session.rs).
/// Ce module sera activé en v0.4.0 pour les connecteurs community (process stdio).
